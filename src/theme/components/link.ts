const variants = {
  menu: () => ({
    borderRadius: "lg",
    _hover: {
      textDecoration: "none",
      bg: "bg-subtle",
    },
  }),
};

// eslint-disable-next-line import/no-anonymous-default-export
export default {
  variants,
};
